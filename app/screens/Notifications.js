import React from 'react';
import {Text, SafeAreaView} from 'react-native';

const Notifications = props => {
  // console.log(props.route.params.id);
  return (
    <SafeAreaView>
      <Text>Notifications SCFREEN </Text>
    </SafeAreaView>
  );
};

export default Notifications;
